[package]
name = "valkey-pubsub"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true }
dashmap = { workspace = true }
bytes = { workspace = true }
valkey-proto = { path = "../proto" }
